# 贡献指南

本项目是静态作品导航与互动作品集合。

- 首页入口为根目录 `index.html`，保持无需构建即可访问。
- HTML、CSS 和 JavaScript 使用 2 空格缩进，文件使用 UTF-8 编码。
- 页面内容以中文为主，使用语义化 HTML，保证键盘操作及移动端可用。
- 作品导航和图片使用相对路径，兼容部署在域名根目录或子目录。
- 新增作品时提供名称、入口路径及封面图片；封面统一放在 `assets/images/`。
- 修改首页时避免变更独立作品内部文件，不引入非必要依赖。
- 提交前检查所有作品链接和图片路径，验证桌面与手机布局及筛选功能。
